import {AuthEndpointComponent} from './auth-endpoint/auth-endpoint.component';
import {Routes} from '@angular/router';

export const routes: Routes = [
  { path: '', component : AuthEndpointComponent},
];
